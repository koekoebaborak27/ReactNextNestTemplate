# JWT署名キー（本番環境では必ず長いランダム文字列に変更すること）
JWT_SECRET=

# データベース接続URL
# SQLiteの場合: file:./dev.db
# PostgreSQLの場合: postgresql://user:password@localhost:5432/mapdb
DATABASE_URL=

# バックエンドのポート
PORT=3001

# フロントエンドのURL（CORS許可対象）
FRONTEND_URL=http://localhost:3000
