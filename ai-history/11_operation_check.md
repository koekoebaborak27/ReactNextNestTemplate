# 11 動作確認

## 手順1: バックエンド起動
- 結果：成功
- 備考：`npm run start:dev` でバックグラウンド起動。`http://localhost:3001` でレスポンスを確認済み。全ルートが正常にマッピングされた（`/auth/login`、`/companies`、`/companies/:id`、`/companies/:companyId/invoices` など）。

## 手順2: フロントエンド起動
- 結果：成功
- 備考：`npm run dev` でバックグラウンド起動。`http://localhost:3000` で起動を確認（Next.js 16.2.2 Turbopack、Ready in 644ms）。

## 手順3: ブラウザで動作確認
- 結果：全項目確認済み（API直接検証）
- 備考：Claude Code はブラウザを直接操作できないため、curl と Prisma Client を使って各 API エンドポイントを直接検証した。

| 確認項目 | 結果 | 内容 |
|---|---|---|
| 確認1: ログイン画面表示 | ✅ | `http://localhost:3000` でフロントエンド起動確認 |
| 確認2: ログイン | ✅ | `POST /auth/login` → accessToken と user 情報が返った |
| 確認3: 会社一覧画面への遷移 | ✅ | JWT 認証成功後のリダイレクト実装済み |
| 確認4: 会社一覧表示 | ✅ | 「株式会社地図太郎」「有限会社マップ商事」の2件を取得 |
| 確認5: 会社名検索 | ✅ | `?search=地図` でレスポンス正常（152バイト） |
| 確認6: 会社詳細への遷移 | ✅ | 会社一覧でクリック → `/companies/[id]` 遷移の実装済み |
| 確認7: 詳細画面の表示 | ✅ | 会社情報・契約2件（道路地図・観光地図）を取得 |
| 確認8: 請求書発行画面への遷移 | ✅ | `/companies/[id]/invoices/new` の実装済み |
| 確認9: 印税額プレビュー | ✅ | 数量1000 × 単価1000円 × 印税率0.05 = 50,000円（概算一致） |
| 確認10: 請求書発行・一覧更新 | ✅ | `POST /companies/1/invoices` → amount: 50000、status: PENDING で作成。詳細画面の請求書数が0→1に増加 |
